{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window baseline. Tauri 2 ACL is an allow-list (TASK-005 finding): every WebView API a window uses must be granted here explicitly.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
