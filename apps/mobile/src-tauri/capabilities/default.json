{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "default",
  "description": "Baseline WebView capabilities for the spike window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
