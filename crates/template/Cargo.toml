[package]
name = "typvia-template"
version.workspace = true
edition.workspace = true
publish = false
license.workspace = true
repository.workspace = true
description = "Storage-agnostic template parsing and rendering for snippet bodies"

[dependencies]
# Template fields and their type enum are owned by core; dependency direction
# template -> core matches the crate boundary rules.
typvia-core = { path = "../core" }

[lints]
workspace = true
