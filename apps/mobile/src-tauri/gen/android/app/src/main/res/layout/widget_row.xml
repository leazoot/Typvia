<?xml version="1.0" encoding="utf-8"?>
<!-- One widget row: two-letter mono type mark + title, nothing else (no
     body preview, no per-type coloring). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:minHeight="40dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/widget_row_mark"
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:importantForAccessibility="no"
        android:textColor="@color/tv_meta"
        android:textSize="11sp" />

    <TextView
        android:id="@+id/widget_row_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:importantForAccessibility="no"
        android:maxLines="1"
        android:textColor="@color/tv_ink"
        android:textSize="14sp" />
</LinearLayout>
