{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike windows need core event/window APIs for latency acks.",
  "windows": ["main", "panel", "cold*"],
  "permissions": ["core:default"]
}
