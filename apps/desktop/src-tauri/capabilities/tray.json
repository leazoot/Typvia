{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "Resident menu-bar / tray card window: baseline core access so it can listen for the tray:show event and invoke the tray commands. Tauri 2 ACL is an allow-list, so the card is granted separately from main.",
  "windows": ["tray"],
  "permissions": ["core:default"]
}
